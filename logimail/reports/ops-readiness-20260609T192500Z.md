# LogiMail Ops Readiness Report

Generated at: 2026-06-09T19:25:00Z

## VPS

- Provider panel: GreenCloud `logivn-prod-vps-01` is active.
- Hostname over SSH: `vps01.logivn.com`.
- SSH user: `deploy`; direct root SSH is disabled.
- `deploy` has passwordless sudo.
- Public IPv4: `103.199.19.144`.
- OS: Ubuntu 24.04.4 LTS, kernel `6.8.0-106-generic`.
- CPU/RAM/storage from provider: 4 CPU, 8192 MB RAM, 60 GB storage.
- Runtime snapshot: 43 GB free on `/`, about 5.2 GiB available RAM, Docker 29.5.2, Nginx 1.24.0.

## Existing VPS Services

Existing Docker/Nginx ports observed:

- `127.0.0.1:3001` Uptime Kuma.
- `127.0.0.1:3002` Grafana.
- `127.0.0.1:3100` gateway.
- `127.0.0.1:3200` socket.
- `127.0.0.1:3300` AI service.
- `127.0.0.1:3400` image service.
- `127.0.0.1:3500` worker.
- `127.0.0.1:3600` tenant Telegram bot.
- `127.0.0.1:3650` platform Telegram bot.
- `127.0.0.1:5540` RedisInsight.
- `127.0.0.1:9090` Prometheus.
- `127.0.0.1:9093` Alertmanager.
- Public `80/443` belong to Nginx.

LogiMail planned ports are free locally:

- `25`, `465`, `587`, `110`, `143`, `993`, `995` mail transport.
- `3000` LogiMail PWA upstream.
- `8787` LogiMail API upstream.
- `8081` BillionMail/RoundCube HTTP upstream.
- `8443` BillionMail internal HTTPS/admin upstream.
- `25432` BillionMail Postgres loopback.
- `26379` BillionMail Redis loopback.

## Cloudflare DNS

Dashboard account/zone visible for `logivn.com`.

Current public DNS report saved at:

```text
logimail/reports/dns-public-20260609T192010Z.txt
```

Key findings:

- `mail.logivn.com`, `smtp.logivn.com`, and `imap.logivn.com` currently fall through wildcard/Vercel.
- `api.logivn.com`, `ws.logivn.com`, `worker.logivn.com`, and `monitor.logivn.com` point to `103.199.19.144`.
- Root MX still uses Cloudflare Email Routing: `route1/route2/route3.mx.cloudflare.net`.
- Root SPF is `v=spf1 include:_spf.mx.cloudflare.net ~all`.
- `_dmarc.logivn.com` and `default._domainkey.logivn.com` fall through wildcard/Vercel.
- Existing DKIM `cf2024-1._domainkey.logivn.com` is present.
- `send.logivn.com` MX points to AWS SES feedback SMTP.

Safe first DNS change:

```text
A mail.logivn.com -> 103.199.19.144, DNS only
```

Do not change root MX/SPF/DMARC until BillionMail is installed, tested, and cutover is explicitly approved.

## GreenCloud Reverse DNS

- GreenCloud reverse DNS field appears empty for IPv4 `103.199.19.144`.
- Provider requires a matching forward DNS record before setting reverse DNS.
- Correct order: create exact `A mail.logivn.com -> 103.199.19.144`, wait for resolution, then set rDNS to `mail.logivn.com`.

## Supabase

- Project ref: `tfhqatvevbrbzaaqjhfa`.
- Dashboard project: `qr-restaurant-saas`, region Singapore.
- Read-only SQL check returned no rows for schemas `logimail` and `logimail_private`.

Pending safe migration target:

```text
logimail/supabase/migrations/20260609000000_logimail_mvp_schema.sql
```

This migration creates isolated LogiMail schemas, metadata tables, RLS policies, and grants. It does not modify existing LogiVN production tables.

## Recommended Next Production Actions

1. Run Supabase LogiMail migration on project `tfhqatvevbrbzaaqjhfa`.
2. In Supabase Data API settings, expose `logimail` only; do not expose `logimail_private`.
3. Add Cloudflare exact `A mail.logivn.com -> 103.199.19.144`, DNS only.
4. After DNS resolves, set GreenCloud rDNS for `103.199.19.144` to `mail.logivn.com`.
5. Install/configure BillionMail in shared-VPS mode only after the above is confirmed.
6. Keep root MX/SPF/DMARC unchanged until mail sending/receiving is verified.
