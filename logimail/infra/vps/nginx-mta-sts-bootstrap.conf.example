# Temporary HTTP-only server used before the first mta-sts certificate exists.
server {
  listen 80;
  listen [::]:80;
  server_name mta-sts.logivn.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 404;
  }
}
