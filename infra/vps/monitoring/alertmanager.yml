global:
  resolve_timeout: 5m

route:
  receiver: logivn-gateway
  group_by: ["alertname", "severity"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: logivn-gateway
    webhook_configs:
      - url: http://gateway:3100/alerts
        send_resolved: true
