/* Fonts cho cây customer v2. Tái dùng đúng các loader của landing v2
 * (Sora / Inter / JetBrains Mono) để không tải trùng bộ font và để
 * các biến CSS --font-v2-* khớp với customer-tokens-v2.css. */
export { fontDisplay, fontSans, fontMono, fontVars } from "@/components/landing-v2/fonts";
